version: '3.8'
services:
  backend:
    build: ./docker
    container_name: budget-backend
    ports:
      - "4000:4000"
  frontend:
    build: ./docker
    container_name: budget-frontend
    ports:
      - "3000:3000"
